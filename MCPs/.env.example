# Copy to ~/.config/agents-config/mcp.env (chmod 600) and fill in the values.
# Secrets live outside the checkout so the repo can be shared read-only.

NEW_RELIC_API_KEY=

# Jira attachments MCP
ATLASSIAN_API_TOKEN=
ATLASSIAN_EMAIL=
JIRA_ATTACHMENT_OUTPUT_ROOT=
JIRA_DEFAULT_SITE=
